# build (generated)

Compose for this mode is **generated from canonical**, not stored here.

Run from stargate-suite root: `make gen` (Web API), then use `build/build/docker-compose.yml`. See [../../README.md](../../README.md).
