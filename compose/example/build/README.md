# build (generated)

Compose for this mode is **generated from canonical**, not stored here.

Run from stargate-suite root: `make gen` or `go run ./cmd/suite gen all`, then use `build/build/docker-compose.yml`. See [../../README.md](../../README.md).
