# English strings (Stargate Suite generate page)
en:
  title: "Stargate Suite - Generate docker-compose & .env"
  subtitle: "Select compose type(s) and options, then click Generate to download."
  composeType: "Compose Type"
  modeTraefik: "Full Traefik (all-in-one)"
  modeTraefikDesc: "Generate full compose with Traefik, Stargate, Warden, Herald."
  modeHeraldOnly: "Herald only"
  modeHeraldOnlyDesc: "Generate only Traefik + Herald compose."
  modeWardenOnly: "Warden only"
  modeWardenOnlyDesc: "Generate only Traefik + Warden compose."
  modeStargateOnly: "Stargate + protected service only"
  modeStargateOnlyDesc: "Generate only Traefik + Stargate + protected example service compose."
  configOptions: "Options"
  healthCheckSection: "Health check"
  traefikNetworkSection: "Traefik network"
  exposePortsSection: "Expose ports"
  containerPrefixSection: "Container & naming"
  healthCheck: "Enable health checks"
  healthCheckDesc: "Enable healthcheck in generated compose for services."
  healthCheckIntervalLabel: "Check interval"
  healthCheckIntervalDesc: "Health check interval, e.g. 10s, 30s, 1m; leave empty for compose default."
  healthCheckStartPeriodLabel: "Start period"
  healthCheckStartPeriodDesc: "Grace period before failed checks count, e.g. 10s, 15s; leave empty for default."
  traefikNetwork: "Configure Traefik network"
  traefikNetworkDesc: "Attach services to Traefik external network in compose."
  traefikNetworkNameLabel: "Traefik network name"
  traefikNetworkNameDesc: "Network name used in compose; must match existing Traefik."
  exposePorts: "Expose ports to host"
  exposePortsDesc: "Map service ports to host for local access and debugging."
  portHeraldLabel: "Herald host port"
  portHeraldDesc: "Host port for Herald service; default 8082."
  portWardenLabel: "Warden host port"
  portWardenDesc: "Host port for Warden service; default 8081."
  portHeraldRedisLabel: "Herald Redis host port"
  portHeraldRedisDesc: "Host port for Herald Redis; default 6379."
  containerNamePrefixLabel: "Container name prefix"
  containerNamePrefixDesc: "Prefix for generated container names; leave empty for default."
  containerPrefixPlaceholder: "the-gate- (leave empty for default)"
  optionalChannelsSection: "Optional channels"
  dingtalkEnabledLabel: "Enable DingTalk channel (herald-dingtalk)"
  dingtalkEnabledDesc: "When checked, generated compose includes herald-dingtalk; Herald can call it over HTTP for verification code push."
  smtpEnabledLabel: "Enable SMTP channel (herald-smtp)"
  smtpEnabledDesc: "When checked, generated compose includes herald-smtp; Herald can call it over HTTP to send email verification codes."
  redisStorage: "Redis storage"
  redisVolume: "Use Docker named volume"
  redisVolumeDesc: "Store Redis data in Docker named volume for backup and migration."
  redisPath: "Use host bind path"
  redisPathDesc: "Bind Redis data to host directory; set paths below."
  heraldRedisPathLabel: "Herald Redis data path"
  heraldRedisPathDesc: "Host path for Herald Redis data."
  wardenRedisPathLabel: "Warden Redis data path"
  wardenRedisPathDesc: "Host path for Warden Redis data."
  serviceEnv: "Service env vars"
  stargateDomainRouting: "Domain & routing"
  authHostLabel: "Auth page host"
  authHostDesc: "Host domain for auth entry, login page and callback."
  stargateDomainLabel: "Stargate domain"
  stargateDomainDesc: "Domain used by Stargate auth service."
  protectedDomainLabel: "Protected service domain"
  protectedDomainDesc: "Domain of the service protected behind Stargate auth."
  stargatePrefixLabel: "Stargate route prefix"
  stargatePrefixDesc: "Traefik route prefix for auth service."
  protectedPrefixLabel: "Protected route prefix"
  protectedPrefixDesc: "Traefik route prefix for protected service."
  userHeaderLabel: "User header name"
  userHeaderDesc: "Header name for forwarded user info to downstream after auth."
  stargateUi: "UI & features"
  languageLabel: "UI language"
  languageDesc: "Language for Stargate login page and UI."
  loginPageTitleLabel: "Login page title"
  loginPageTitleDesc: "Title text shown on Stargate login page."
  loginPageFooterLabel: "Login page footer"
  loginPageFooterDesc: "Copyright or footer text at bottom of login page."
  cookieDomainLabel: "Cookie domain"
  cookieDomainDesc: "Set for cross-subdomain login; leave empty for no domain restriction."
  wardenEnabledDesc: "Enable Warden allowlist user service."
  heraldEnabledDesc: "Enable Herald OTP/verification code service."
  wardenCacheTtlLabel: "Warden cache TTL (sec)"
  wardenCacheTtlDesc: "Seconds Stargate caches Warden user info."
  sessionStorageDesc: "Use Redis for session storage (otherwise cookie)."
  sessionStorageRedisAddrLabel: "Session Redis address"
  sessionStorageRedisAddrDesc: "Redis address (host:port) when session storage is enabled."
  sessionStorageRedisPasswordLabel: "Session Redis password"
  sessionStorageRedisPasswordDesc: "Leave empty if Redis has no auth."
  auditLogEnabledLabel: "Enable audit log"
  auditLogEnabledDesc: "Record login and auth audit logs."
  auditLogFormatLabel: "Audit log format"
  auditLogFormatDesc: "Audit log output format: json or text."
  debugDesc: "Enable extra debug logging."
  optionalSensitive: "Optional (sensitive)"
  wardenEnabledLabel: "Enable Warden"
  heraldEnabledLabel: "Enable Herald"
  heraldTotpEnabledLabel: "Enable herald-totp (TOTP 2FA)"
  heraldTotpEnabledDesc: "Enable herald-totp; Stargate will support TOTP enroll/verify and backup codes."
  heraldTotpBaseUrlLabel: "herald-totp URL"
  heraldTotpBaseUrlDesc: "URL for Stargate to call herald-totp, e.g. http://herald-totp:8084."
  heraldTotpApiKeyLabel: "herald-totp API key"
  heraldTotpApiKeyDesc: "Must match API_KEY in herald-totp container; empty = no auth (dev only)."
  sessionStorageEnabledLabel: "Enable session storage"
  debugLabel: "Debug"
  languageZh: "zh"
  languageEn: "en"
  languageFr: "fr"
  languageIt: "it"
  languageJa: "ja"
  languageDe: "de"
  languageKo: "ko"
  auditLogFormatJson: "json"
  auditLogFormatText: "text"
  logLevelInfo: "info"
  logLevelDebug: "debug"
  logLevelWarn: "warn"
  logLevelError: "error"
  modeDefault: "DEFAULT (remote-first)"
  modeRemoteFirst: "REMOTE_FIRST"
  modeOnlyRemote: "ONLY_REMOTE"
  modeOnlyLocal: "ONLY_LOCAL"
  modeLocalFirst: "LOCAL_FIRST"
  modeRemoteFirstAllowFailed: "REMOTE_FIRST_ALLOW_REMOTE_FAILED"
  modeLocalFirstAllowFailed: "LOCAL_FIRST_ALLOW_REMOTE_FAILED"
  providerSoft: "soft"
  providerStrict: "strict"
  passwordsLabel: "PASSWORDS"
  passwordsDesc: "Optional password list; empty uses compose default; change in production."
  wardenLogLevelLabel: "Log level"
  wardenLogLevelDesc: "Log level for both Warden and Herald."
  modeLabel: "Run mode"
  modeDesc: "Warden data merge mode: DEFAULT / ONLY_REMOTE / ONLY_LOCAL / LOCAL_FIRST / remote-first allow failed / local-first allow failed."
  wardenApiKeyLabel: "API key"
  wardenApiKeyDesc: "API key for Stargate to call Warden; empty = compose default; change in prod."
  intervalLabel: "Poll interval (sec)"
  intervalDesc: "Interval in seconds for Warden to fetch user data from remote."
  wardenRedisPasswordLabel: "Warden Redis password"
  wardenRedisPasswordDesc: "Leave empty if Warden Redis has no auth."
  wardenRedisPasswordFileLabel: "Warden Redis password file"
  wardenRedisPasswordFileDesc: "Optional; path to password file (overrides password)."
  wardenRedisEnabledLabel: "Enable Redis"
  wardenRedisEnabledDesc: "Whether Warden uses Redis; in ONLY_LOCAL default is false unless REDIS or this is set."
  wardenRemoteConfigLabel: "Remote config URL"
  wardenRemoteConfigDesc: "Remote URL for Warden to fetch user data in REMOTE/HYBRID modes."
  wardenRemoteKeyLabel: "Remote config auth"
  wardenRemoteKeyDesc: "Optional; e.g. Bearer token for remote API, sent as Authorization header."
  wardenHttpMaxIdleConnsLabel: "HTTP max idle conns"
  wardenHttpMaxIdleConnsDesc: "Warden HTTP client max idle connections for remote fetch; default 100."
  wardenHttpInsecureTlsLabel: "Skip TLS verify"
  wardenHttpInsecureTlsDesc: "Development only; skip TLS cert verification when fetching remote."
  wardenDataFileLabel: "Local data file path"
  wardenDataFileDesc: "Path inside container for ONLY_LOCAL user data file; must match volume mount."
  wardenHttpTimeoutLabel: "HTTP timeout (sec)"
  wardenHttpTimeoutDesc: "HTTP request timeout in seconds when Warden fetches remote config or data."
  heraldTestModeLabel: "Test mode"
  heraldTestModeDesc: "When on, do not send real SMS/email; for local testing."
  heraldRedisPasswordLabel: "Herald Redis password"
  heraldRedisPasswordDesc: "Leave empty if Herald Redis has no auth."
  heraldRedisDbLabel: "Herald Redis DB"
  heraldRedisDbDesc: "Redis database number for Herald; usually 0."
  challengeExpiryLabel: "Challenge expiry"
  challengeExpiryDesc: "OTP challenge validity, e.g. 5m."
  codeLengthLabel: "Code length"
  codeLengthDesc: "Number of digits for OTP code (typically 4–8)."
  maxAttemptsLabel: "Max attempts"
  maxAttemptsDesc: "Max wrong verification attempts per challenge before lock."
  providerFailurePolicyLabel: "Provider failure policy"
  providerFailurePolicyDesc: "soft: continue; strict: reject when send fails."
  resendCooldownLabel: "Resend cooldown"
  resendCooldownDesc: "Wait time before resending same challenge, e.g. 60s."
  lockoutDurationLabel: "Lockout duration"
  lockoutDurationDesc: "Duration to lock after max attempts, e.g. 10m."
  idempotencyKeyTtlLabel: "Idempotency key TTL"
  idempotencyKeyTtlDesc: "Herald idempotency record TTL; 0 = use challenge expiry, e.g. 5m."
  allowedPurposesLabel: "Allowed purposes"
  allowedPurposesDesc: "Comma-separated purposes, e.g. login,reset,bind,stepup."
  serviceNameLabel: "Service name"
  serviceNameDesc: "Herald service identifier for HMAC etc.; default herald."
  heraldHmacKeysLabel: "HMAC keys (JSON)"
  heraldHmacKeysDesc: "Optional; multi-key JSON e.g. {\"key-id\":\"secret\"} for key rotation."
  rateLimitPerUserLabel: "Rate limit per user (/hour)"
  rateLimitPerUserDesc: "Max challenges per user_id per hour."
  rateLimitPerIpLabel: "Rate limit per IP (/min)"
  rateLimitPerIpDesc: "Max challenges per IP per minute."
  rateLimitPerDestLabel: "Rate limit per destination (/hour)"
  rateLimitPerDestDesc: "Max challenges per phone/email per hour."
  heraldApiKeyLabel: "Herald API key"
  heraldApiKeyDesc: "API key for Stargate to call Herald; empty = compose default."
  heraldHmacLabel: "HMAC secret"
  heraldHmacDesc: "HMAC secret for Stargate–Herald request signing; empty = compose default."
  imageVersionsSection: "Image versions"
  imageVersionsSectionDesc: "Centralized image versions for the three services and Redis; written to generated .env."
  heraldImageLabel: "Herald image"
  heraldImageDesc: "Herald (OTP/verification) service image."
  wardenImageLabel: "Warden image"
  wardenImageDesc: "Warden (allowlist) service image."
  stargateImageLabel: "Stargate image"
  stargateImageDesc: "Stargate (Forward Auth) service image."
  heraldRedisImageLabel: "Herald Redis image"
  heraldRedisImageDesc: "Redis image used by Herald."
  wardenRedisImageLabel: "Warden Redis image"
  wardenRedisImageDesc: "Redis image used by Warden."
  protectedImageLabel: "Protected service image (whoami)"
  protectedImageDesc: "Example service image behind Stargate Forward Auth; used for E2E and demos."
  providerEnv: "Provider (Herald channel)"
  providerEnvDesc: "Optional: Herald calls these via HTTP for OTP/notification; one row per provider."
  heraldDingtalkName: "herald-dingtalk (DingTalk)"
  heraldDingtalkImageLabel: "herald-dingtalk image"
  heraldDingtalkImageDesc: "DingTalk OTP push service image; Herald calls it via HTTP."
  heraldDingtalkApiUrlLabel: "Herald: herald-dingtalk URL"
  heraldDingtalkApiUrlDesc: "For Herald config: herald-dingtalk service URL, e.g. http://herald-dingtalk:8083."
  heraldDingtalkApiKeyLabel: "Herald / herald-dingtalk API key"
  heraldDingtalkApiKeyDesc: "Optional; if herald-dingtalk sets API_KEY, Herald must send X-API-Key."
  dingtalkAppKeyLabel: "DingTalk app key"
  dingtalkAppKeyDesc: "DingTalk app key used by herald-dingtalk."
  dingtalkAppSecretLabel: "DingTalk app secret"
  dingtalkAppSecretDesc: "DingTalk app secret used by herald-dingtalk."
  dingtalkAgentIdLabel: "DingTalk Agent ID"
  dingtalkAgentIdDesc: "Agent ID for DingTalk work notification in herald-dingtalk."
  dingtalkLookupModeLabel: "Lookup mode"
  dingtalkLookupModeDesc: "none = to is userid only; mobile = to can be userid or 11-digit mobile (requires Contact.User.mobile)."
  heraldDingtalkIdempotencyTtlLabel: "Idempotency TTL (sec)"
  heraldDingtalkIdempotencyTtlDesc: "herald-dingtalk idempotency cache TTL; default 300."
  heraldSmtpName: "herald-smtp (Email SMTP)"
  heraldSmtpImageLabel: "herald-smtp image"
  heraldSmtpImageDesc: "Email verification code send service image; Herald calls it via HTTP."
  heraldSmtpApiUrlLabel: "Herald: herald-smtp URL"
  heraldSmtpApiUrlDesc: "For Herald config: herald-smtp service URL, e.g. http://herald-smtp:8085."
  heraldSmtpApiKeyLabel: "Herald / herald-smtp API key"
  heraldSmtpApiKeyDesc: "Optional; if herald-smtp sets API_KEY, Herald must send X-API-Key."
  smtpHostLabel: "SMTP host"
  smtpHostDesc: "SMTP server hostname; required for sending."
  smtpPortLabel: "SMTP port"
  smtpPortDesc: "SMTP port, usually 587 (STARTTLS) or 465 (SSL)."
  smtpUserLabel: "SMTP user"
  smtpUserDesc: "SMTP auth username; leave empty for anonymous if server allows."
  smtpPasswordLabel: "SMTP password"
  smtpPasswordDesc: "SMTP auth password; leave empty for no auth."
  smtpFromLabel: "From address"
  smtpFromDesc: "Sender email address; required for sending."
  smtpUseStarttlsLabel: "Use STARTTLS"
  smtpUseStarttlsDesc: "Whether to use STARTTLS; usually true."
  heraldSmtpIdempotencyTtlLabel: "Idempotency TTL (sec)"
  heraldSmtpIdempotencyTtlDesc: "herald-smtp idempotency cache TTL; default 300."
  heraldTotpSection: "herald-totp (TOTP 2FA)"
  heraldTotpPortLabel: "Listen port"
  heraldTotpPortDesc: "herald-totp listen port, e.g. :8084."
  heraldTotpRedisAddrLabel: "Redis address"
  heraldTotpRedisAddrDesc: "Redis address for herald-totp (can share with Herald)."
  heraldTotpEncryptionKeyLabel: "Encryption key (32 bytes)"
  heraldTotpEncryptionKeyDesc: "AES-256 key for TOTP secret storage; required; use random 32 bytes in production."
  heraldTotpExposeSecretLabel: "enroll/start returns secret_base32"
  heraldTotpExposeSecretDesc: "When true, enroll/start response includes secret_base32 for manual authenticator entry."
  heraldTotpLogLevelLabel: "Log level"
  heraldTotpLogLevelDesc: "herald-totp log level."
  heraldTotpImageLabel: "herald-totp image"
  heraldTotpImageDesc: "herald-totp (TOTP enroll/verify/backup codes) service image."
  portHeraldTotpLabel: "herald-totp host port"
  portHeraldTotpDesc: "Host port for herald-totp; default 8084."
  portHeraldSmtpLabel: "herald-smtp host port"
  portHeraldSmtpDesc: "Host port for herald-smtp; default 8085."
  btnGenerate: "Generate"
  generating: "Generating..."
  resultSuccess: "Done. Download:"
  resultErrorNeedMode: "Please select at least one compose type."
  requestFailed: "Request failed: "
