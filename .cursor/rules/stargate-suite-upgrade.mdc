---
description: Stargate Suite v0.10 upgrade rules
globs: **/*
alwaysApply: true
---

# Stargate Suite 升级执行规则

适用于 `soulteary/stargate-suite` 的升级改造（详见 `PLAN.md` 与 `docs/upgrade/`）。

## 范围与契约

- 除非任务明确说明，只修改 `soulteary/stargate-suite`。
- 把 Stargate v1.0.0、Warden v1.1.0、Herald v1.1.0 的 Tag 视为**只读契约来源**，不复制其业务实现。
- 组件版本、端口、健康路径必须来自 `config/components.yaml`（单一权威来源），不得在其他文件重新硬编码。

## 安全底线（不可为通过测试而放宽）

- 不得为让测试通过而降低 production 校验。
- HMAC v1 必须默认关闭。
- 内部服务与 Redis 在 production 不得发布宿主机端口。
- 绝不提交 secret、生成的 `.env` 文件、`build/` 生成目录，或用于生产的测试凭证。

## 一致性与文档

- CLI 与 Web UI 必须调用同一套 compose 生成与校验函数。
- 每一处行为变化都需要配套测试，以及相关中英文文档更新。
- 保留与本次任务无关的用户改动，不重排/格式化无关文件。

## 流程

- 报告完成前，先运行当前 PR 工作包（`docs/upgrade/pr-*.md`）中定义的验证命令。
- 未经明确指示，不得 commit、push、merge、重跑付费 workflow 或创建 Release。
